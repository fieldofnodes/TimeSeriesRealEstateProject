#Packages to load into the library
library(ggplot2)
library(extrafont)

#Getting fonts ready - Uncomment the following lines if needed.
#font_import() # Import all fonts
#fonts() # Print list of all fonts
#loadfonts()

library(grid)
library(useful)